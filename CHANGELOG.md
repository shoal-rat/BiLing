# 更新记录 / Changelog

本文档遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [1.4.0] - 2026-07-27

### 新增

- **简拼与缩写**：词库新增声母索引（格式 2，重建为 111 MB，改经
  Git LFS 分发）。整串首字母直接出词（`jldx` → 吉林大学、`zgrm` →
  中国人民），全拼 + 首字母混合可拼句（`beijinghy` → 北京还有）；
  字母串本身是合法全拼时全拼优先（`fan` → 饭 不受影响）。
- **LoRA 个性化**：`scripts/train_lora.sh` 一条命令完成 导出选择记录 →
  mlx-lm 本地微调 Qwen3-0.6B-Base → 融合导出 GGUF → 量化 Q4_K_M →
  装载；守护进程在下一次（懒）加载时自动改用个人模型，状态行标注
  "个人模型"，删除文件即回退。推理层同时支持标准 GGUF LoRA 适配器
  （`adapters/qwen-lora.gguf` 或 `BILING_LORA_PATH`，`BILING_LORA_SCALE`
  调强度），适配器加载失败不影响打字。
- `biling-cli --export-training-data DIR` 把加密学习库导出为 mlx-lm
  训练格式；`--adapter` 指定适配器。
- 模式判定改进：以拼音为主、尾部带声母的串（beijinghy）不再被误判为
  英文输入。

## [1.3.0] - 2026-07-27

### 性能与能耗

- Qwen 上下文的 KV 缓存跨请求复用：上下文没变就完全不解码，变了只解码
  新增后缀；上下文裁剪改为带滞回（3072 字符收到 1536），窗口在两次裁剪
  之间保持字节稳定，缓存持续命中。
- 全词表 softmax 改用 Accelerate（vDSP/vvexpf）向量化，替换掉每次按键
  上千万次的标量 exp 循环。
- 词典查询改为常驻预编译语句；句子束搜索按位置记忆化词典命中，并用键
  区间探测提前终止无效前缀，长句每键的 SQLite 往返从数百次降到几十次。
- 模型守护进程改为懒加载 + 空闲卸载：登录时不再加载权重，10 分钟无请求
  自动释放整个模型（守护进程回落到数 MB），下次请求经 mmap 秒级恢复。
- 移除输入法进程的 15 秒周期健康轮询与每次成功排序后的状态往返；状态
  改为按需刷新，空闲时两个进程都不再定时唤醒。
- 单次排序候选从 12 个提高到 16 个。

### 新增

- Qwen 上下文改为优先读取**输入框内光标前的真实文本**（每次组词开始时
  向宿主应用读取一次，最多 600 字符；不支持的客户端回退到会话内已提交
  文本）——续写别人写了一半的内容时，模型第一次真正"看得见前文"。
- 拉丁字母识别升级为约 260 条带大小写还原的精选词表（Claude、OpenAI、
  ChatGPT、AI、GDP、IPO、xswl、yyds、emo、Tom、Emma……）+ macOS 系统
  20 万词表，并按"是合法拼音则中文优先，无真实词条则专名优先"排序
  （`ai` → 爱 第一、AI 前几名可见；`openai` → OpenAI 第一）。
- 低电量模式自动把会话切换给苹果自带拼音；未启用苹果拼音时退回纯词典
  模式（不调用模型），候选面板明确提示。

### 修复与体验

- 修复短拼音被单字母词条拼接碾压的问题（`fan` 的第一候选现在是 饭，
  不再是 发嗯）：句子束搜索对单字母词典键（嗯、呣等）施加惩罚。
- Qwen 请求失败不再清空候选列表：词典候选保持可用，面板仅切换状态徽标。
- CLI 与输入法共用同一个混分函数（`CandidateBlender`），`biling-cli` 打印
  的顺序与实际输入时一致；新增 `--engine-only` 直接检验词典引擎。
- 语言模型权重按上下文校准：有已提交上下文 0.42，冷启动 0.25——没有
  证据时词频占上风，有上下文时模型说了算。
- 英文补全从 19 个硬编码词升级为分层词表：常用技术词即时响应，macOS
  系统词表（约 20 万词）在首次需要时后台加载；仍是拼音的尾串只匹配
  精选词，避免生僻英文词混进中文整句。

### 文档与示例

- 中英 README 全部重写；新增拼词流水线、学习循环、方案对比三组
  中英双语 SVG，重绘首页横幅与架构图。
- 发布示例改为 `jilindaxuelajixuexiao → 吉林大学垃圾学校`（整句合成
  回归，安装器与测试均以此为门槛）。

### 清理

- 删除未使用的 `LatticeDAG`、`CandidatePage`、`SyllableEdge.prior` 与
  重复的 `scripts/register.swift`（安装器改用 App 自带的注册命令）。

## [1.2.0] - 2026-07-27

### 新增

- 输入法与 Qwen 两个用户域 LaunchAgent，均在登录时启动并保持运行。
- 输入法进程级 Qwen 健康检查、15 秒周期探测与 1–15 秒指数退避重连。
- 无需构建工具的 Apple silicon 在线安装器，从固定 Git LFS 提交取得并校验模型。
- 默认中文、可切换英文的论文式 README，以及中文 SVG 横幅和架构图。
- Git LFS 模型分发、公开 CI、贡献指南、安全政策与私密漏洞报告说明。

### 修复

- 将输入源从 ASCII-capable Unicode 模式修正为非拉丁
  `smSimpChinese`，恢复 macOS“中/英”键与 Caps Lock 的系统切换语义。
- 安装器现在保留并恢复安装前使用的输入源。
- XPC interruption 不再停留在永久“Qwen 不可用”状态。

### 验证

- 19 项 Swift 测试通过。
- 1,440,094 条发布词汇通过只读 SQLite 回归门槛。
- 安装后 CLI 与真实输入法进程的 Qwen 健康检查通过。
- 故障注入后 launchd 自动换 PID 重启模型，`jilindaxue → 吉林大学` 仍排名第一。

## [1.1.0] - 2026-07-26

- 修复 InputMethodKit 启动崩溃与空白候选条。
- 候选窗改用 AppKit 原生文本控件。
- 集成万象拼音与 Rime 主词库，以及强制启用的本地 Qwen 排序器。
