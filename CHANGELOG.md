# 更新记录 / Changelog

本文档遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [1.3.0] - 2026-07-27

### 新增

- **简拼与缩写**：词库为每个词条预存声母码（`dx` → 大学）与混合码
  （首音节全拼 + 其余声母，`meiy` → 没有、`kongt` → 空调），各建
  `(码, 权重)` 索引。整串简拼直接出词（`jldx` → 吉林大学），词级组合
  覆盖真实的快速输入（`jilin·dx·meiy·kongt` → 吉林大学没有空调）。
- **中英混排**：英文词可以出现在句子中间而不只是结尾
  （`economicslajizhuanye` → economics 垃圾专业，
  `yongvscodexiedaima` → 用 VS Code 写代码）。
- **LoRA 个性化**：`scripts/train_lora.sh` 一条命令完成 导出选择记录 →
  mlx-lm 本地微调 Qwen3-0.6B-Base → 融合导出 GGUF → 量化 → 装载；
  守护进程下次加载自动改用个人模型并在状态行标注"个人模型"，删除文件
  即回退。推理层同时支持标准 GGUF LoRA 适配器（`BILING_LORA_PATH`）。
- **评测程序**：`biling-cli --evaluate` 在 72 条标注语料上给出
  top-1/top-5/MRR 与延迟分布，按类别拆分。

### 改进

- **打分模型重写**：此前每个候选来源各有一套自造分数（`log1p(w)+10`、
  `束分数/段数+2`、字面串常数 `18`/`100`、学习项 `24+log1p(c)*4`），量纲
  互不相通，改一处即坏一处。现统一为单一对数概率：
  `score = Σ[log P(词) + log P(书写形式)]`。"段数少者胜"、"缩写是回退"、
  "拉丁词平等参赛"由模型自然导出，不再需要专门规则；路径分数不再除以段数
  （该操作恰好抵消分词所需的长度偏好）。
- 实测（72 条语料）：仅词典 76.4% top-1；加本地模型 77.8%；再加光标前文
  **81.9% top-1 / 90.3% top-5**，上下文消歧类 54.5% → 81.8%。
- **性能**：`mixed` 部分索引缺少谓词导致全表扫描，缩写整句从 3035 ms 降至
  14.2 ms（214×）。

### 修复

- 句中拉丁词若展开形比原键长（`vscode` → `VS Code`），越界崩溃。
- 学习记录与候选面板此前看不到"个人模型"标记。

## [1.2.0] - 2026-07-27

### 新增

- 输入法与 Qwen 两个用户域 LaunchAgent，均在登录时启动并保持运行。
- 输入法进程级 Qwen 健康检查、15 秒周期探测与 1–15 秒指数退避重连。
- 无需构建工具的 Apple silicon 在线安装器，从固定 Git LFS 提交取得并校验模型。
- 默认中文、可切换英文的论文式 README，以及中文 SVG 横幅和架构图。
- Git LFS 模型分发、公开 CI、贡献指南、安全政策与私密漏洞报告说明。

### 修复

- 将输入源从 ASCII-capable Unicode 模式修正为非拉丁
  `smSimpChinese`，恢复 macOS“中/英”键与 Caps Lock 的系统切换语义。
- 安装器现在保留并恢复安装前使用的输入源。
- XPC interruption 不再停留在永久“Qwen 不可用”状态。

### 验证

- 19 项 Swift 测试通过。
- 1,440,094 条发布词汇通过只读 SQLite 回归门槛。
- 安装后 CLI 与真实输入法进程的 Qwen 健康检查通过。
- 故障注入后 launchd 自动换 PID 重启模型，`jilindaxue → 吉林大学` 仍排名第一。

## [1.1.0] - 2026-07-26

- 修复 InputMethodKit 启动崩溃与空白候选条。
- 候选窗改用 AppKit 原生文本控件。
- 集成万象拼音与 Rime 主词库，以及强制启用的本地 Qwen 排序器。
